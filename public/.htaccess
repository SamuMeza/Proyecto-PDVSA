<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /sistema_pdvsa/

    # Redirect everything to public/index.php (front controller)
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ public/index.php [QSA,L]
</IfModule>
